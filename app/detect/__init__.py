"""Detection: run a frame through the TensorRT engine and read out boxes.

engine.py      TensorRT engine I/O, no image or detection semantics
postprocess.py letterbox in, boxes back out in source-frame pixels
"""
